{
  "name": "ecoa",
  "version": "1.0.0",
  "description": "A major redesign of the traditional Tec's primary satisfaction survey: ECOA",
  "main": "schema.js",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "db:reset": "npm run db:reset --workspace=server",
    "build": "npm run build --workspace=web",
    "start": "npm run start --workspace=server",
    "lint": "prettier --check ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mysql2": "^3.2.0"
  },
  "devDependencies": {
    "prettier": "^2.8.8"
  }
}
